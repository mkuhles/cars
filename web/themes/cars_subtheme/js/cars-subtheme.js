/**
 * @file
 * cars-subtheme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.carsSubtheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
